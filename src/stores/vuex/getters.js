export default {
    selectedComponent(state) {
      return state.selectedComponent;
    },
    filteredData(state) {
      return state.filteredData;
    },
    inputHasChanged(state) {
      return state.inputHasChanged;
    },
    countries(state) {
      return state.countries;
    },
    regions(state) {
      return state.regions;
    },
    states(state) {
      return state.states;
    },
    towncities(state) {
      return state.towncities;
    },
    barangays(state) {
      return state.barangays;
    }
};